package com.example.ruireutov.organiser.task.taskList.taskViews;

import android.content.Context;
import android.database.Cursor;
import android.view.View;

public class ScheduledTaskView extends ATaskView{
    public ScheduledTaskView(View view, Context context, Cursor cursor) {
        super(view, context, cursor);
    }
}
